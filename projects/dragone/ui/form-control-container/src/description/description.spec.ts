import { Description } from './description';

describe(Description, () => {
  it('should create an instance', () => {
    const directive = new Description();
    expect(directive).toBeTruthy();
  });
});
