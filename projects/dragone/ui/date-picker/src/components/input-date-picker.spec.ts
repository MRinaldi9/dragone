import { InputDatePicker } from './input-date-picker';

describe(InputDatePicker, () => {
  it('should create an instance', () => {
    const directive = new InputDatePicker();
    expect(directive).toBeTruthy();
  });
});
