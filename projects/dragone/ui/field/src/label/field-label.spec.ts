import { FieldLabel } from './field-label';

describe(FieldLabel, () => {
  it('should create an instance', () => {
    const directive = new FieldLabel();
    expect(directive).toBeTruthy();
  });
});
